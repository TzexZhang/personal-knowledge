# 个人知识库管理系统 - 项目方案

## 📋 项目概述

**项目名称**: Personal Knowledge Base System (PKB)
**项目类型**: 全栈 Web 应用
**开发模式**: 个人项目

### 项目简介

一个功能完整的个人知识库管理系统，帮助用户创建、组织和管理个人知识笔记，支持分类、标签、搜索等功能，提供简洁高效的知识管理体验。

---

## 🛠️ 技术栈

### 前端技术

- **React 18.x**: 用户界面框架
- **Ant Design 5.x**: 企业级 UI 组件库（PC 端）
- **Ant Design Mobile 5.x**: 移动端 UI 组件库
- **React Router v6**: 前端路由管理
- **Axios**: HTTP 请求库
- **React Query**: 数据状态管理
- **Vite**: 前端构建工具
- **Day.js**: 日期处理库
- **@ant-design/icons**: Ant Design 图标库
- **react-markdown**: Markdown 渲染器
- **react-swipeable**: 触摸手势库
- **react-window**: 虚拟滚动库

### 后端技术

- **Python 3.10+**: 后端开发语言
- **FastAPI**: 现代化高性能 Web 框架
- **Uvicorn**: ASGI 服务器
- **SQLAlchemy**: ORM 数据库操作
- **Alembic**: 数据库迁移工具
- **Pydantic**: 数据验证和序列化
- **python-jose**: JWT 令牌处理
- **passlib**: 密码加密（bcrypt）

### 数据库

- **MySQL 8.0+**: 关系型数据库
- **Redis**: 缓存与会话管理（可选）

### 开发工具

- **Git**: 版本控制
- **Postman**: API 测试
- **VSCode**: 开发 IDE

---

## 📊 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                      客户端层                            │
│       React 18 + Ant Design 5.x + 主题系统               │
└──────────────────────┬──────────────────────────────────┘
                       │ HTTP/REST API
┌──────────────────────┴──────────────────────────────────┐
│                      服务端层                            │
│        FastAPI + SQLAlchemy + JWT                       │
└──────────────────────┬──────────────────────────────────┘
                       │ SQL
┌──────────────────────┴──────────────────────────────────┐
│                      数据层                              │
│                  MySQL 8.0                               │
└─────────────────────────────────────────────────────────┘
```

---

## 🎯 核心功能模块

### 1. 用户认证模块

- 用户注册
- 用户登录
- JWT 认证
- 密码加密存储
- 个人信息管理

### 2. 知识笔记管理

- 创建笔记（Markdown 编辑器）
- 编辑笔记
- 删除笔记
- 笔记列表展示
- 笔记详情查看
- 富文本内容支持

### 3. 分类管理

- 创建分类
- 编辑分类
- 删除分类
- 分类树形结构
- 按分类筛选笔记

### 4. 标签系统

- 添加标签
- 编辑标签
- 删除标签
- 按标签筛选笔记
- 标签云展示

### 5. 搜索功能

- 全文搜索笔记
- 按标题搜索
- 按内容搜索
- 按标签搜索
- 高级筛选

### 6. 统计分析

- 笔记数量统计
- 分类统计
- 标签使用统计
- 最近编辑记录

### 7. 主题系统

- 明暗主题切换（Light/Dark Mode）
- 主题自动检测（跟随系统）
- 主题偏好持久化存储
- 平滑过渡动画效果
- 自定义主题色配置
- 紧凑/舒适/宽松模式切换

### 8. 移动端适配

- 响应式布局（手机/平板/PC）
- 底部标签栏导航
- 抽屉式侧边栏
- 下拉刷新 & 上拉加载
- 触摸手势支持（左滑/右滑）
- 虚拟滚动（大列表优化）
- PWA 支持（可安装到桌面）
- 离线访问支持
- 安全区域适配（刘海屏）

---

## 🗄️ 数据库设计

### 用户表 (users)

```sql
CREATE TABLE users (
    id INT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) UNIQUE NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    avatar VARCHAR(255),
    theme_preference VARCHAR(20) DEFAULT 'light',
    primary_color VARCHAR(20) DEFAULT '#1890ff',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### 分类表 (categories)

```sql
CREATE TABLE categories (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id INT NOT NULL,
    name VARCHAR(100) NOT NULL,
    description TEXT,
    parent_id INT DEFAULT NULL,
    color VARCHAR(20),
    sort_order INT DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    FOREIGN KEY (parent_id) REFERENCES categories(id) ON DELETE SET NULL
);
```

### 标签表 (tags)

```sql
CREATE TABLE tags (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id INT NOT NULL,
    name VARCHAR(50) NOT NULL,
    color VARCHAR(20),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    UNIQUE KEY unique_tag (user_id, name)
);
```

### 笔记表 (notes)

```sql
CREATE TABLE notes (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id INT NOT NULL,
    category_id INT,
    title VARCHAR(200) NOT NULL,
    content LONGTEXT,
    is_favorite BOOLEAN DEFAULT FALSE,
    view_count INT DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    FOREIGN KEY (category_id) REFERENCES categories(id) ON DELETE SET NULL
);
```

### 笔记标签关联表 (note_tags)

```sql
CREATE TABLE note_tags (
    id INT PRIMARY KEY AUTO_INCREMENT,
    note_id INT NOT NULL,
    tag_id INT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (note_id) REFERENCES notes(id) ON DELETE CASCADE,
    FOREIGN KEY (tag_id) REFERENCES tags(id) ON DELETE CASCADE,
    UNIQUE KEY unique_note_tag (note_id, tag_id)
);
```

---

## 🔌 API 接口设计

### 认证接口

```
POST   /api/auth/register     # 用户注册
POST   /api/auth/login        # 用户登录
GET    /api/auth/profile      # 获取个人信息
PUT    /api/auth/profile      # 更新个人信息
POST   /api/auth/logout       # 用户登出
```

### 分类接口

```
GET    /api/categories        # 获取分类列表
POST   /api/categories        # 创建分类
GET    /api/categories/:id    # 获取分类详情
PUT    /api/categories/:id    # 更新分类
DELETE /api/categories/:id    # 删除分类
```

### 标签接口

```
GET    /api/tags              # 获取标签列表
POST   /api/tags              # 创建标签
GET    /api/tags/:id          # 获取标签详情
PUT    /api/tags/:id          # 更新标签
DELETE /api/tags/:id          # 删除标签
```

### 笔记接口

```
GET    /api/notes             # 获取笔记列表（支持分页、筛选）
POST   /api/notes             # 创建笔记
GET    /api/notes/:id         # 获取笔记详情
PUT    /api/notes/:id         # 更新笔记
DELETE /api/notes/:id         # 删除笔记
GET    /api/notes/search      # 搜索笔记
```

---

## 📁 项目目录结构

```
personal-knowledge-base/
├── backend/                      # Python 后端
│   ├── app/
│   │   ├── __init__.py          # FastAPI 应用初始化
│   │   ├── config.py            # 配置文件
│   │   ├── models/              # 数据库模型
│   │   │   ├── __init__.py
│   │   │   ├── user.py
│   │   │   ├── category.py
│   │   │   ├── tag.py
│   │   │   └── note.py
│   │   ├── routers/             # 路由控制器
│   │   │   ├── __init__.py
│   │   │   ├── auth.py
│   │   │   ├── categories.py
│   │   │   ├── tags.py
│   │   │   └── notes.py
│   │   ├── schemas/             # Pydantic 数据模型
│   │   │   ├── __init__.py
│   │   │   ├── user.py
│   │   │   ├── category.py
│   │   │   ├── tag.py
│   │   │   └── note.py
│   │   ├── utils/               # 工具函数
│   │   │   ├── __init__.py
│   │   │   ├── jwt.py
│   │   │   └── security.py
│   │   └── alembic/             # 数据库迁移文件
│   ├── requirements.txt         # Python 依赖
│   ├── main.py                  # 应用入口
│   └── .env                     # 环境变量
│
├── frontend/                     # React 前端
│   ├── src/
│   │   ├── components/          # 公共组件
│   │   │   ├── layout/          # 布局组件
│   │   │   │   ├── Header.jsx
│   │   │   │   ├── Sidebar.jsx
│   │   │   │   ├── Footer.jsx
│   │   │   │   └── MobileNavigation.jsx  # 移动端底部导航
│   │   │   ├── NoteCard.jsx
│   │   │   ├── MarkdownEditor.jsx
│   │   │   ├── ThemeToggle.jsx   # 主题切换按钮
│   │   │   └── mobile/           # 移动端专用组件
│   │   │       ├── SwipeableCard.jsx
│   │   │       ├── PullToRefresh.jsx
│   │   │       └── VirtualList.jsx
│   │   ├── pages/               # 页面组件
│   │   │   ├── Login.jsx
│   │   │   ├── Register.jsx
│   │   │   ├── Dashboard.jsx
│   │   │   ├── NoteList.jsx
│   │   │   ├── NoteDetail.jsx
│   │   │   └── Settings.jsx     # 设置页（含主题设置）
│   │   ├── contexts/            # Context 状态管理
│   │   │   ├── AuthContext.jsx
│   │   │   └── ThemeContext.jsx # 主题上下文
│   │   ├── hooks/               # 自定义 Hooks
│   │   │   ├── useBreakpoint.js
│   │   │   └── useTheme.js
│   │   ├── services/            # API 服务
│   │   │   └── api.js
│   │   ├── utils/               # 工具函数
│   │   │   └── helpers.js
│   │   ├── themes/              # 主题配置
│   │   │   ├── light.js         # 亮色主题
│   │   │   ├── dark.js          # 暗色主题
│   │   │   └── index.js         # 主题导出
│   │   ├── styles/              # 样式文件
│   │   │   ├── mobile.css       # 移动端样式
│   │   │   └── responsive.css   # 响应式样式
│   │   ├── App.jsx
│   │   ├── main.jsx
│   │   └── index.css
│   ├── public/                  # 静态资源
│   │   ├── manifest.json        # PWA 配置
│   │   ├── service-worker.js    # Service Worker
│   │   └── icons/               # 应用图标
│   ├── package.json
│   └── vite.config.js
│
└── README.md                     # 项目说明文档
```

---

## 🚀 开发计划

### 第一阶段：项目搭建（1-2天）

- [x] 初始化项目结构
- [x] 配置开发环境
- [x] 设计并创建数据库表
- [x] 搭建基础前后端框架

### 第二阶段：后端开发（3-5天）

- [x] 实现用户认证模块
- [x] 实现分类管理 CRUD
- [x] 实现标签管理 CRUD
- [x] 实现笔记管理 CRUD
- [x] 实现搜索功能
- [x] 编写 API 测试

### 第三阶段：前端开发（5-7天）

- [x] 实现登录注册页面
- [x] 搭建 Ant Design 主题系统
- [x] 实现明暗主题切换功能
- [x] 实现主界面布局（三栏布局）
- [x] 实现笔记列表和详情页
- [x] 实现分类和标签管理
- [x] 实现 Markdown 编辑器
- [x] 实现搜索功能
- [x] 实现主题设置页面
- [ ] 移动端布局适配
- [ ] 移动端底部导航栏
- [ ] 抽屉式侧边栏
- [ ] 下拉刷新和上拉加载
- [ ] 触摸手势支持
- [ ] 虚拟滚动优化
- [ ] 响应式设计适配

### 第四阶段：优化测试（2-3天）

- [x] 代码优化和重构
- [x] 性能优化
- [x] 安全性检查
- [x] 功能测试和 Bug 修复
- [x] 用户体验优化

### 第五阶段：部署上线（1-2天）

- [x] 服务器配置
- [x] 数据库部署
- [x] 应用部署
- [x] 域名配置

---

## 🎨 主题系统设计

### 设计理念

基于 Ant Design 5.x 的 Design Tokens 系统，提供完整的明暗主题支持，确保在任何光线下都能提供舒适的阅读体验。

### 主题模式

#### 1. 亮色主题（Light Mode）

**色彩方案：**
```javascript
{
  // 主色调
  primary: '#1890ff',        // Ant Design 默认蓝
  success: '#52c41a',        // 成功绿
  warning: '#faad14',        // 警告橙
  error: '#ff4d4f',          // 错误红
  info: '#1890ff',           // 信息蓝

  // 背景色
  bgLayout: '#f5f5f5',       // 布局背景
  bgContainer: '#ffffff',    // 容器背景
  bgElevated: '#ffffff',     // 浮层背景

  // 文本色
  textPrimary: '#000000d9',  // 主要文字
  textSecondary: '#00000073',// 次要文字
  textTertiary: '#00000040', // 三级文字

  // 边框色
  borderColor: '#d9d9d9',    // 边框
  colorBorderSecondary: '#f0f0f0' // 二级边框
}
```

**特点：**
- 清新明亮，适合白天使用
- 对比度高，易于阅读
- 符合主流审美习惯

#### 2. 暗色主题（Dark Mode）

**色彩方案：**
```javascript
{
  // 主色调
  primary: '#177ddc',        // 暗色模式蓝
  success: '#49aa19',        // 暗色成功绿
  warning: '#d89614',        // 暗色警告橙
  error: '#d32029',          // 暗色错误红
  info: '#177ddc',           // 暗色信息蓝

  // 背景色
  bgLayout: '#141414',       // 布局背景
  bgContainer: '#1f1f1f',    // 容器背景
  bgElevated: '#262626',     // 浮层背景

  // 文本色
  textPrimary: '#ffffffd9',  // 主要文字
  textSecondary: '#ffffff73',// 次要文字
  textTertiary: '#ffffff40', // 三级文字

  // 边框色
  borderColor: '#424242',    // 边框
  colorBorderSecondary: '#303030' // 二级边框
}
```

**特点：**
- 护眼舒适，适合夜间使用
- 减少蓝光刺激
- 降低功耗（OLED 屏幕）

### 主题切换功能

#### 切换方式

1. **手动切换**
   - 顶部导航栏主题切换按钮
   - 设置页面主题选项
   - 快捷键切换（Ctrl/Cmd + Shift + T）

2. **自动切换**
   - 跟随系统主题设置
   - 根据时间自动切换（如：18:00-6:00 自动切换到暗色模式）
   - 地理位置（根据日出日落时间）

#### 技术实现

```javascript
// 主题 Context
import { createContext, useContext, useState, useEffect } from 'react';
import { ConfigProvider, theme } from 'antd';

const ThemeContext = createContext();

export const ThemeProvider = ({ children }) => {
  const [isDark, setIsDark] = useState('light');
  const [primaryColor, setPrimaryColor] = useState('#1890ff');

  // 从 localStorage 读取用户偏好
  useEffect(() => {
    const savedTheme = localStorage.getItem('theme');
    const savedColor = localStorage.getItem('primaryColor');

    if (savedTheme) {
      setIsDark(savedTheme === 'dark');
    } else {
      // 跟随系统
      const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
      setIsDark(prefersDark);
    }

    if (savedColor) {
      setPrimaryColor(savedColor);
    }
  }, []);

  // 监听系统主题变化
  useEffect(() => {
    const mediaQuery = window.matchMedia('(prefers-color-scheme: dark)');
    const handleChange = (e) => {
      if (!localStorage.getItem('theme')) {
        setIsDark(e.matches);
      }
    };

    mediaQuery.addEventListener('change', handleChange);
    return () => mediaQuery.removeEventListener('change', handleChange);
  }, []);

  // 切换主题
  const toggleTheme = (theme) => {
    setIsDark(theme === 'dark');
    localStorage.setItem('theme', theme);
  };

  // 更改主色调
  const changePrimaryColor = (color) => {
    setPrimaryColor(color);
    localStorage.setItem('primaryColor', color);
  };

  const themeConfig = {
    algorithm: isDark ? theme.darkAlgorithm : theme.defaultAlgorithm,
    token: {
      colorPrimary: primaryColor,
      borderRadius: 6,
    },
  };

  return (
    <ThemeContext.Provider value={{ isDark, toggleTheme, primaryColor, changePrimaryColor }}>
      <ConfigProvider theme={themeConfig}>
        {children}
      </ConfigProvider>
    </ThemeContext.Provider>
  );
};

export const useTheme = () => useContext(ThemeContext);
```

### 组件主题适配

#### Ant Design 组件自动适配

Ant Design 5.x 的 Design Tokens 系统会自动处理主题切换：

```jsx
import { Button, Table, Form, Input } from 'antd';

// 所有组件自动适配当前主题
const MyComponent = () => {
  return (
    <div>
      <Button type="primary">主要按钮</Button>
      <Table dataSource={data} />
      <Form>
        <Input placeholder="自动适配主题" />
      </Form>
    </div>
  );
};
```

#### 自定义组件主题适配

```css
/* 使用 CSS 变量实现主题适配 */
.custom-note-card {
  background-color: var(--ant-colorBgContainer);
  border: 1px solid var(--ant-colorBorder);
  color: var(--ant-colorText);
  transition: all 0.3s ease;
}

/* 主题切换动画 */
* {
  transition: background-color 0.3s ease,
              color 0.3s ease,
              border-color 0.3s ease;
}
```

### 布局设计

#### 主界面布局

```
┌────────────────────────────────────────────────────────┐
│  Header 顶部导航栏 [Logo] [搜索] [主题切换] [用户]     │
├──────────┬───────────────────────────┬─────────────────┤
│          │                           │                 │
│  左侧    │      中间内容区            │    右侧编辑区   │
│  侧边栏  │                           │                 │
│          │   - 笔记列表               │   - Markdown   │
│ - 分类   │   - 标签筛选               │     编辑器      │
│ - 标签   │   - 搜索结果               │                 │
│ - 统计   │                           │   - 实时预览    │
│          │                           │                 │
│  (可收起)│                           │                 │
└──────────┴───────────────────────────┴─────────────────┘
```

#### 响应式断点

```javascript
const breakpoints = {
  xs: '480px',   // 手机（竖屏）
  sm: '576px',   // 大屏手机（横屏）
  md: '768px',   // 平板（竖屏）
  lg: '992px',   // 平板（横屏）/ 小屏电脑
  xl: '1200px',  // 电脑
  xxl: '1600px'  // 大屏电脑
};

// 响应式布局策略
// - xs (< 576px): 单列布局，抽屉式侧边栏
// - md (≥ 768px): 双列布局，左侧边栏 + 内容区
// - lg (≥ 992px): 三列布局，完整显示
```

### 移动端适配设计

#### 移动端布局策略

**1. 手机端布局（xs < 576px）**

```
┌─────────────────────────────┐
│  Header [☰] [标题] [🔍] [👤]│
├─────────────────────────────┤
│                             │
│      笔记列表 / 详情         │
│                             │
│   - 卡片式笔记展示          │
│   - 上拉加载更多            │
│   - 下拉刷新                │
│                             │
└─────────────────────────────┘
┌─────────────────────────────┐
│  [抽屉式侧边栏 - 滑入]      │
│                             │
│  - 分类                     │
│  - 标签                     │
│  - 统计                     │
│  - 设置                     │
│                             │
└─────────────────────────────┘
```

**2. 平板端布局（md ≥ 768px）**

```
┌─────────────────────────────────────┐
│  Header [Logo] [搜索] [主题] [用户] │
├──────────┬──────────────────────────┤
│          │                          │
│  左侧    │      笔记列表/详情        │
│  侧边栏  │                          │
│          │   - 卡片/列表视图         │
│ - 分类   │   - 详情页（抽屉式）      │
│ - 标签   │                          │
│          │                          │
└──────────┴──────────────────────────┘
```

#### 移动端导航设计

**1. 底部标签栏（Bottom Navigation）**

```jsx
import { TabBar } from 'antd-mobile';

const MobileNavigation = () => {
  return (
    <TabBar>
      <TabBar.Item key="home" icon={<AppOutline />}>
        笔记
      </TabBar.Item>
      <TabBar.Item key="category" icon={<UnorderedListOutline />}>
        分类
      </TabBar.Item>
      <TabBar.Item key="search" icon={<SearchOutline />}>
        搜索
      </TabBar.Item>
      <TabBar.Item key="profile" icon={<UserOutline />}>
        我的
      </TabBar.Item>
    </TabBar>
  );
};
```

**2. 抽屉式侧边栏（Drawer）**

```jsx
import { Drawer } from 'antd';

const MobileSidebar = ({ visible, onClose }) => {
  return (
    <Drawer
      placement="left"
      width={280}
      onClose={onClose}
      open={visible}
      styles={{
        body: { padding: 0 },
      }}
    >
      <Menu mode="inline" defaultSelectedKeys={['1']}>
        <Menu.Item key="1">我的笔记</Menu.Item>
        <Menu.Item key="2">分类管理</Menu.Item>
        <Menu.Item key="3">标签管理</Menu.Item>
      </Menu>
    </Drawer>
  );
};
```

#### 移动端卡片布局

```jsx
import { Card, Tag, Space } from 'antd';
import { useBreakpoint } from 'antd';

const NoteCard = ({ note }) => {
  const screens = useBreakpoint();

  return (
    <Card
      hoverable
      style={{
        marginBottom: 16,
        borderRadius: screens.xs ? 8 : 12,
      }}
      bodyStyle={{
        padding: screens.xs ? 12 : 24,
      }}
    >
      <h3 style={{ marginBottom: 8, fontSize: screens.xs ? 16 : 18 }}>
        {note.title}
      </h3>
      <p style={{ marginBottom: 12, fontSize: screens.xs ? 14 : 14 }}>
        {note.excerpt}
      </p>
      <Space wrap>
        <Tag color="blue">{note.category}</Tag>
        {note.tags.map(tag => (
          <Tag key={tag}>{tag}</Tag>
        ))}
      </Space>
      <div style={{ marginTop: 12, fontSize: screens.xs ? 12 : 13, color: '#999' }}>
        {note.updatedAt}
      </div>
    </Card>
  );
};
```

#### 移动端列表/网格切换

```jsx
import { Segmented, Card, List } from 'antd';
import { AppstoreOutlined, BarsOutlined } from '@ant-design/icons';

const NoteListView = () => {
  const [viewMode, setViewMode] = useState('list');

  return (
    <>
      <Segmented
        value={viewMode}
        onChange={setViewMode}
        options={[
          { label: '列表', value: 'list', icon: <BarsOutlined /> },
          { label: '卡片', value: 'card', icon: <AppstoreOutlined /> },
        ]}
        style={{ marginBottom: 16 }}
      />

      {viewMode === 'list' ? (
        <List
          dataSource={notes}
          renderItem={note => (
            <List.Item>
              <List.Item.Meta
                title={note.title}
                description={note.excerpt}
              />
            </List.Item>
          )}
        />
      ) : (
        <div className="grid-view">
          {notes.map(note => (
            <NoteCard key={note.id} note={note} />
          ))}
        </div>
      )}
    </>
  );
};
```

#### 触摸优化

**1. 增大点击区域**

```css
/* 移动端最小触摸目标 44x44px */
.mobile-button {
  min-height: 44px;
  min-width: 44px;
  padding: 12px 16px;
}

/* 卡片间距 */
.mobile-card {
  margin: 8px 0;
}

/* 列表项高度 */
.mobile-list-item {
  min-height: 56px;
  padding: 12px 16px;
}
```

**2. 手势支持**

```javascript
import { useSwipeable } from 'react-swipeable';

const SwipeableCard = ({ note, onDelete, onEdit }) => {
  const handlers = useSwipeable({
    onSwipedLeft: () => onDelete(note.id),
    onSwipedRight: () => onEdit(note.id),
    preventDefaultTouchmoveEvent: true,
    trackMouse: true,
  });

  return (
    <div {...handlers} className="swipeable-card">
      <Card>{note.title}</Card>
    </div>
  );
};
```

**3. 下拉刷新 & 上拉加载**

```javascript
import { PullToRefresh, InfiniteScroll } from 'antd-mobile';

const MobileNoteList = () => {
  const [notes, setNotes] = useState([]);
  const [hasMore, setHasMore] = useState(true);
  const [loading, setLoading] = useState(false);

  // 下拉刷新
  const onRefresh = async () => {
    setLoading(true);
    const freshNotes = await fetchNotes();
    setNotes(freshNotes);
    setLoading(false);
  };

  // 上拉加载
  const loadMore = async () => {
    const moreNotes = await fetchMoreNotes(notes.length);
    setNotes([...notes, ...moreNotes]);
    setHasMore(moreNotes.length > 0);
  };

  return (
    <PullToRefresh onRefresh={onRefresh} loading={loading}>
      <InfiniteScroll loadMore={loadMore} hasMore={hasMore}>
        {notes.map(note => (
          <NoteCard key={note.id} note={note} />
        ))}
      </InfiniteScroll>
    </PullToRefresh>
  );
};
```

#### 移动端表单优化

```jsx
import { Form, Input, Button, Space } from 'antd-mobile';

const NoteFormMobile = () => {
  const [form] = Form.useForm();

  return (
    <Form
      form={form}
      layout="vertical"
      footer={
        <Button block color="primary" size="large">
          保存笔记
        </Button>
      }
    >
      <Form.Item
        name="title"
        label="标题"
        rules={[{ required: true, message: '请输入标题' }]}
      >
        <Input placeholder="请输入笔记标题" clearable />
      </Form.Item>

      <Form.Item
        name="content"
        label="内容"
        rules={[{ required: true, message: '请输入内容' }]}
      >
        <TextArea
          placeholder="请输入笔记内容"
          rows={10}
          showCount
          maxLength={10000}
        />
      </Form.Item>

      <Form.Item name="category" label="分类">
        <Selector
          options={[
            { label: '技术', value: 'tech' },
            { label: '生活', value: 'life' },
            { label: '工作', value: 'work' },
          ]}
        />
      </Form.Item>
    </Form>
  );
};
```

#### 移动端搜索体验

```jsx
import { SearchBar } from 'antd-mobile';
import { debounce } from 'lodash';

const MobileSearch = () => {
  const [searchText, setSearchText] = useState('');

  const handleSearch = debounce(async (value) => {
    const results = await searchNotes(value);
    // 显示搜索结果
  }, 300);

  return (
    <div className="mobile-search">
      <SearchBar
        placeholder="搜索笔记..."
        value={searchText}
        onChange={setSearchText}
        onSearch={handleSearch}
        autoFocus
      />
      {/* 搜索历史 */}
      {/* 热门搜索 */}
      {/* 搜索结果 */}
    </div>
  );
};
```

#### 响应式图片

```jsx
import { Image } from 'antd';

const ResponsiveImage = ({ src, alt }) => {
  return (
    <Image
      src={src}
      alt={alt}
      preview={{
        // 移动端全屏预览
        toolbarRender: () => null,
      }}
      style={{
        maxWidth: '100%',
        height: 'auto',
        display: 'block',
      }}
      loading="lazy"
    />
  );
};
```

#### 虚拟滚动（大列表优化）

```javascript
import { VariableSizeList as List } from 'react-window';

const VirtualNoteList = ({ notes }) => {
  const getItemSize = (index) => {
    // 根据内容动态计算高度
    return notes[index].height || 100;
  };

  return (
    <List
      height={window.innerHeight - 64}
      itemCount={notes.length}
      itemSize={getItemSize}
      width="100%"
    >
      {({ index, style }) => (
        <div style={style}>
          <NoteCard note={notes[index]} />
        </div>
      )}
    </List>
  );
};
```

#### 移动端性能优化

**1. 懒加载组件**

```javascript
import { lazy, Suspense } from 'react';

const NoteEditor = lazy(() => import('./NoteEditor'));
const StatisticsChart = lazy(() => import('./StatisticsChart'));

function App() {
  return (
    <Suspense fallback={<div>加载中...</div>}>
      <NoteEditor />
      <StatisticsChart />
    </Suspense>
  );
}
```

**2. 图片懒加载**

```jsx
<img
  src={thumbnail}
  data-src={fullSize}
  loading="lazy"
  alt="笔记图片"
/>
```

**3. 防抖节流**

```javascript
import { debounce, throttle } from 'lodash';

// 搜索输入防抖
const debouncedSearch = debounce(handleSearch, 300);

// 滚动事件节流
const throttledScroll = throttle(handleScroll, 100);
```

**4. 减少 DOM 渲染**

```javascript
// 使用虚拟化
import { List } from 'react-virtualized';

// 分页加载
const [page, setPage] = useState(1);
const [notes, setNotes] = useState([]);
```

#### 移动端交互反馈

**1. 触摸反馈**

```css
/* 点击反馈 */
.mobile-button:active {
  transform: scale(0.98);
  opacity: 0.8;
  transition: all 0.1s;
}

/* 卡片点击效果 */
.mobile-card:active {
  background-color: var(--ant-colorBgTextHover);
}

/* 长按效果 */
.mobile-long-press {
  animation: pulse 0.3s;
}

@keyframes pulse {
  0% { transform: scale(1); }
  50% { transform: scale(0.95); }
  100% { transform: scale(1); }
}
```

**2. Toast 提示**

```javascript
import { Toast } from 'antd-mobile';

const showToast = () => {
  Toast.show({
    content: '保存成功',
    icon: 'success',
    duration: 2000,
  });
};
```

**3. 加载状态**

```javascript
import { DotLoading } from 'antd-mobile';

<Loading>
  <DotLoading />
  加载中...
</Loading>
```

#### 响应式字体

```css
/* 基础字体大小 */
html {
  font-size: 16px; /* 桌面端 */
}

/* 移动端 */
@media (max-width: 576px) {
  html {
    font-size: 14px;
  }
}

/* 标题字体 */
h1 { font-size: 2rem; }
h2 { font-size: 1.5rem; }
h3 { font-size: 1.25rem; }

@media (max-width: 576px) {
  h1 { font-size: 1.5rem; }
  h2 { font-size: 1.25rem; }
  h3 { font-size: 1.125rem; }
}
```

#### 移动端特殊功能

**1. PWA 支持**

```javascript
// manifest.json
{
  "name": "个人知识库",
  "short_name": "知识库",
  "description": "个人知识管理系统",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#1890ff",
  "icons": [
    {
      "src": "/icon-192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "/icon-512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}
```

**2. 添加到主屏幕提示**

```javascript
// 检测是否在移动端
const isMobile = /iPhone|iPad|iPod|Android/i.test(navigator.userAgent);

if (isMobile && !window.matchMedia('(display-mode: standalone)').matches) {
  // 显示"添加到主屏幕"提示
  showInstallPrompt();
}
```

**3. 离线支持（Service Worker）**

```javascript
// service-worker.js
const CACHE_NAME = 'pkb-v1';
const urlsToCache = [
  '/',
  '/static/css/main.css',
  '/static/js/main.js',
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME)
      .then((cache) => cache.addAll(urlsToCache))
  );
});

self.addEventListener('fetch', (event) => {
  event.respondWith(
    caches.match(event.request)
      .then((response) => response || fetch(event.request))
  );
});
```

#### 横屏适配

```css
@media (orientation: landscape) {
  .mobile-header {
    position: fixed;
    height: 48px; /* 减小高度 */
  }

  .mobile-content {
    padding-top: 48px;
  }
}
```

#### 安全区域适配（刘海屏）

```css
/* iPhone X 及以上 */
@supports (padding: max(0px)) {
  .mobile-header {
    padding-top: max(12px, env(safe-area-inset-top));
    padding-bottom: max(12px, env(safe-area-inset-bottom));
    padding-left: max(12px, env(safe-area-inset-left));
    padding-right: max(12px, env(safe-area-inset-right));
  }
}
```

#### 移动端适配清单

**布局适配：**
- ✅ 底部导航栏（TabBar）
- ✅ 抽屉式侧边栏
- ✅ 卡片式内容展示
- ✅ 列表/网格视图切换
- ✅ 响应式间距

**交互优化：**
- ✅ 下拉刷新
- ✅ 上拉加载
- ✅ 左滑右滑操作
- ✅ 长按菜单
- ✅ 触摸反馈

**性能优化：**
- ✅ 虚拟滚动
- ✅ 图片懒加载
- ✅ 组件懒加载
- ✅ 防抖节流
- ✅ 缓存策略

**体验提升：**
- ✅ PWA 支持
- ✅ 离线访问
- ✅ 添加到主屏幕
- ✅ 安全区域适配
- ✅ 横屏适配

**样式适配：**
- ✅ 响应式字体
- ✅ 弹性布局
- ✅ 网格系统
- ✅ 媒体查询
- ✅ 设备像素比适配

### Ant Design 组件使用

#### 核心组件清单

**布局组件：**
- `Layout` - 页面布局框架
- `Sider` - 侧边栏
- `Header` - 顶部导航
- `Content` - 内容区域
- `Footer` - 页脚

**导航组件：**
- `Menu` - 菜单导航
- `Breadcrumb` - 面包屑
- `Tabs` - 标签页
- `Pagination` - 分页

**数据录入：**
- `Form` - 表单
- `Input` - 输入框
- `TextArea` - 文本域
- `Select` - 选择器
- `DatePicker` - 日期选择
- `Upload` - 上传

**数据展示：**
- `Table` - 表格
- `List` - 列表
- `Card` - 卡片
- `Tree` - 树形控件
- `Tag` - 标签
- `Avatar` - 头像

**反馈组件：**
- `Message` - 全局提示
- `Modal` - 对话框
- `Notification` - 通知提示
- `Popconfirm` - 气泡确认框
- `Progress` - 进度条

**导航组件：**
- `Dropdown` - 下拉菜单
- `Drawer` - 抽屉

### 页面布局示例

```jsx
import { Layout, Menu, Button, theme } from 'antd';
import { BulbOutlined, BulbFilled } from '@ant-design/icons';

const { Header, Sider, Content } = Layout;

const MainLayout = () => {
  const { isDark, toggleTheme } = useTheme();
  const {
    token: { colorBgContainer },
  } = theme.useToken();

  return (
    <Layout style={{ minHeight: '100vh' }}>
      <Sider
        width={240}
        style={{ background: colorBgContainer }}
      >
        <Menu mode="inline" defaultSelectedKeys={['1']}>
          <Menu.Item key="1">我的笔记</Menu.Item>
          <Menu.Item key="2">分类管理</Menu.Item>
          <Menu.Item key="3">标签管理</Menu.Item>
        </Menu>
      </Sider>

      <Layout>
        <Header style={{ padding: '0 24px', background: colorBgContainer }}>
          <div style={{ display: 'flex', justifyContent: 'space-between' }}>
            <div>Logo</div>
            <Button
              icon={isDark ? <BulbFilled /> : <BulbOutlined />}
              onClick={() => toggleTheme(isDark ? 'light' : 'dark')}
            >
              主题切换
            </Button>
          </div>
        </Header>

        <Content style={{ margin: '24px' }}>
          {/* 主内容区域 */}
        </Content>
      </Layout>
    </Layout>
  );
};
```

### 主题切换动画

```css
/* 主题切换过渡效果 */
.theme-transition {
  transition: background-color 0.3s cubic-bezier(0.645, 0.045, 0.355, 1),
              color 0.3s cubic-bezier(0.645, 0.045, 0.355, 1),
              border-color 0.3s cubic-bezier(0.645, 0.045, 0.355, 1);
}

/* 暗色模式特殊样式 */
html.dark {
  color-scheme: dark;
}

/* 滚动条样式 */
::-webkit-scrollbar {
  width: 8px;
  height: 8px;
}

::-webkit-scrollbar-thumb {
  background: var(--ant-colorBorder);
  border-radius: 4px;
}

::-webkit-scrollbar-thumb:hover {
  background: var(--ant-colorTextSecondary);
}
```

### 主题自定义

#### 用户自定义主题色

```javascript
// 预设主题色
const presetColors = [
  { name: '拂晓蓝', value: '#1890ff' },
  { name: '薄暮', value: '#722ed1' },
  { name: '火山', value: '#fa541c' },
  { name: '日暮', value: '#faad14' },
  { name: '明青', value: '#13c2c2' },
  { name: '极光绿', value: '#52c41a' },
  { name: '极客蓝', value: '#2f54eb' },
  { name: '酱紫', value: '#722ed1' },
];

// 主题色选择器
const ColorPicker = () => {
  const { primaryColor, changePrimaryColor } = useTheme();

  return (
    <div>
      <h3>主题色</h3>
      <Space>
        {presetColors.map(color => (
          <Button
            key={color.value}
            style={{ backgroundColor: color.value }}
            onClick={() => changePrimaryColor(color.value)}
          >
            {color.name}
          </Button>
        ))}
        <Input
          type="color"
          value={primaryColor}
          onChange={(e) => changePrimaryColor(e.target.value)}
        />
      </Space>
    </div>
  );
};
```

### 性能优化

1. **主题缓存** - localStorage 存储用户偏好
2. **CSS-in-JS** - Ant Design 5.x 使用 CSS-in-JS，避免样式冲突
3. **按需加载** - 只加载使用到的组件样式
4. **静态生成** - 主题样式在构建时预生成

---

## 🔒 安全性考虑

1. **认证安全**
   - JWT Token 认证
   - 密码使用 bcrypt 加密
   - Token 过期时间控制

2. **数据安全**
   - SQL 注入防护（使用 ORM）
   - XSS 防护（输入验证和转义）
   - CORS 配置

3. **接口安全**
   - 请求频率限制
   - 输入数据验证
   - 错误信息处理

---

## 📈 扩展功能（未来规划）

- [ ] 笔记导出（PDF、Markdown）
- [ ] 笔记分享功能
- [ ] 多人协作编辑
- [ ] 文件附件支持
- [ ] 笔记版本历史
- [ ] 全文检索优化
- [ ] 数据备份与恢复
- [ ] 移动端适配优化

---

## 📚 学习价值

通过本项目，您将掌握：

1. **前端技能**
   - React 函数组件和 Hooks
   - React Router 路由管理
   - React Query 数据状态管理
   - Ant Design 5.x 组件库使用
   - Ant Design Mobile 移动端组件库
   - 主题系统设计与实现（明暗模式）
   - Design Tokens 设计理念
   - 响应式布局设计
   - 移动端适配方案
   - PWA 开发与实践
   - 触摸交互优化
   - 前后端分离架构

2. **后端技能**
   - FastAPI 框架应用开发
   - RESTful API 设计
   - JWT 身份认证
   - SQLAlchemy ORM 操作
   - Pydantic 数据验证
   - 数据库设计原则

3. **全栈开发**
   - 前后端分离开发模式
   - API 接口设计与对接
   - 数据库设计与应用
   - 项目架构与部署
   - Git 版本控制

---

## 🛠️ 快速开始

### 后端启动

```bash
cd backend
pip install -r requirements.txt
cp .env.example .env
# 配置 .env 文件中的数据库连接信息
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### 前端启动

```bash
cd frontend
npm install
npm run dev
```

---

## 🚀 Zeabur 一站式部署方案（推荐）

### 为什么选择 Zeabur？

**Zeabur** 是一个现代化的应用部署平台，专为个人开发者和小团队设计，提供全栈应用的一站式部署解决方案。

#### 核心优势

- ✅ **零学习成本**: 自动识别项目技术栈，无需复杂配置
- ✅ **全球加速**: 自带全球 CDN，访问速度极快
- ✅ **免费额度充足**: 个人项目可长期免费使用
- ✅ **一键部署**: 连接 GitHub 仓库，自动部署
- ✅ **MySQL 原生支持**: 内置 MySQL 数据库服务
- ✅ **自动 HTTPS**: 自动配置 SSL 证书
- ✅ **国内访问友好**: 相比其他平台，国内访问速度更快
- ✅ **预览环境**: 每个 PR 自动生成预览链接

### 部署架构

```
GitHub 仓库
    ↓
Zeabur 平台
    ├─ Service 1: React Frontend (Vite)
    ├─ Service 2: Python FastAPI Backend
    └─ Service 3: MySQL Database
```

### 部署前准备

#### 1. 创建配置文件

在项目根目录创建 `zeabur.yml` 配置文件：

```yaml
# zeabur.yml
type: background

# MySQL 数据库
mysql:
  type: mysql
  version: "8.0"
  env:
    - MYSQL_DATABASE=personal_kb

# FastAPI 后端服务
backend:
  type: container
  dockerfile: ./backend/Dockerfile
  env:
    - PORT=8000
    - DATABASE_URL=${mysql.DATABASE_URL}
    - JWT_SECRET_KEY=${JWT_SECRET_KEY}
  depends_on:
    - mysql

# React 前端服务
frontend:
  type: container
  dockerfile: ./frontend/Dockerfile
  env:
    - VITE_API_URL=${backend.domain}
  depends_on:
    - backend
```

#### 2. 后端 Dockerfile

```dockerfile
# backend/Dockerfile
FROM python:3.10-slim

WORKDIR /app

# 安装依赖
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 复制应用代码
COPY . .

# 暴露端口
EXPOSE 8000

# 启动命令
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

#### 3. 前端 Dockerfile

```dockerfile
# frontend/Dockerfile
# 构建阶段
FROM node:18-alpine as builder

WORKDIR /app

# 安装依赖
COPY package*.json ./
RUN npm install

# 构建应用
COPY . .
RUN npm run build

# 生产阶段 - 使用 Nginx
FROM nginx:alpine

# 复制构建产物
COPY --from=builder /app/dist /usr/share/nginx/html

# 复制 Nginx 配置
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

#### 4. Nginx 配置文件

```nginx
# frontend/nginx.conf
server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # SPA 路由支持
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 代理（如果在同一网络）
    location /api {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
}
```

### 部署步骤

#### 方式一：通过 Web 界面部署（推荐新手）

1. **注册 Zeabur 账号**
   - 访问 [zeabur.com](https://zeabur.com)
   - 使用 GitHub 账号登录

2. **创建新项目**
   - 点击 "New Project"
   - 选择 "Deploy from GitHub"
   - 授权 Zeabur 访问您的 GitHub 仓库

3. **添加 MySQL 数据库**
   - 点击 "Add Service"
   - 选择 "Marketplace"
   - 找到并选择 "MySQL"
   - Zeabur 会自动创建数据库实例

4. **部署后端服务**
   - 点击 "Add Service"
   - 选择 "Git" → 选择您的仓库
   - 设置 Root Path 为 `./backend`
   - Zeabur 会自动识别为 Python 项目
   - 设置环境变量：
     - `DATABASE_URL`: 从 MySQL 服务自动获取
     - `JWT_SECRET_KEY`: 生成一个随机密钥
     - `APP_ENV`: `production`

5. **部署前端服务**
   - 再次点击 "Add Service"
   - 选择同一个仓库
   - 设置 Root Path 为 `./frontend`
   - Zeabur 会自动识别为 Node.js 项目
   - 设置环境变量：
     - `VITE_API_URL`: 自动从后端服务获取域名

6. **获取访问地址**
   - 部署完成后，每个服务会获得一个 `*.zeabur.app` 域名
   - 前端地址示例：`https://your-app.zeabur.app`

#### 方式二：通过 Zeabur CLI 部署（推荐进阶）

```bash
# 安装 Zeabur CLI
npm install -g zeabur

# 登录
zeabur login

# 初始化项目
cd personal-knowledge-base
zeabur init

# 部署
zeabur deploy
```

### 环境变量配置

在 Zeabur 控制台中配置以下环境变量：

**后端服务环境变量：**

```bash
# 数据库连接（Zeabur 会自动提供 ${mysql.DATABASE_URL}）
DATABASE_URL=mysql+pymysql://root:password@host:3306/personal_kb

# JWT 密钥（请使用强密码）
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this

# FastAPI 配置
APP_ENV=production
DEBUG=0

# CORS 配置
FRONTEND_URL=https://your-app.zeabur.app
```

**前端服务环境变量：**

```bash
# API 地址（Zeabur 会自动提供 ${backend.domain}）
VITE_API_URL=https://your-backend.zeabur.app
```

### 自定义域名配置

1. **在 Zeabur 控制台**
   - 进入前端服务设置
   - 点击 "Domains"
   - 点击 "Add Custom Domain"
   - 输入您的域名（如：`kb.yourdomain.com`）

2. **配置 DNS**
   - 在您的域名服务商处添加 CNAME 记录：

   ```
   类型: CNAME
   名称: kb
   值: fn.zeabur.com
   ```

3. **自动 HTTPS**
   - Zeabur 会自动为自定义域名配置 SSL 证书
   - 无需手动操作

### 持续部署配置

Zeabur 支持自动持续部署：

1. **自动部署**
   - 默认配置：每次推送到 `main` 分支自动触发部署
   - 可在设置中修改触发分支

2. **预览环境**
   - 每个 Pull Request 自动生成预览环境
   - 适合代码审查和测试

3. **部署钩子**
   - 可在部署前后执行自定义脚本
   - 示例：数据库迁移、缓存清理等

### 监控和日志

Zeabur 提供完善的监控功能：

1. **实时日志**
   - 在控制台查看实时应用日志
   - 支持日志搜索和过滤

2. **资源监控**
   - CPU 使用率
   - 内存使用情况
   - 网络流量统计

3. **告警通知**
   - 配置邮件或 Slack 告警
   - 服务异常时自动通知

### 成本说明

Zeabur 采用按量计费模式：

| 方案       | 配置       | 价格     | 适用场景           |
| ---------- | ---------- | -------- | ------------------ |
| **免费版** | $10/月额度 | 免费     | 个人学习、原型验证 |
| **Pro 版** | $29/月起   | 按量付费 | 生产环境           |
| **团队版** | 定制       | 联系销售 | 企业级应用         |

**免费额度包含：**

- 512MB 内存
- 1GB 存储
- 无限带宽
- 足够运行个人项目

**计费规则（超出免费额度）：**

- 计算资源：$0.00023/秒（约 $0.62/小时）
- 存储：$0.10/GB/月
- 流量：前 100GB 免费

### 实战部署清单

- [ ] 注册 Zeabur 账号并连接 GitHub
- [ ] 创建 `zeabur.yml` 配置文件
- [ ] 创建 `backend/Dockerfile`
- [ ] 创建 `frontend/Dockerfile`
- [ ] 创建 `frontend/nginx.conf`
- [ ] 在 Zeabur 控制台添加 MySQL 服务
- [ ] 部署后端服务并配置环境变量
- [ ] 部署前端服务并配置环境变量
- [ ] 测试前后端连接
- [ ] （可选）配置自定义域名
- [ ] （可选）配置自动部署规则

### 常见问题

**Q: Zeabur 免费版会休眠吗？**
A: 不会。Zeabur 免费版的服务会一直运行，不会因为无流量而休眠。

**Q: 数据库会持久化吗？**
A: 是的。Zeabur 提供持久化存储，重启后数据不会丢失。

**Q: 支持域名备案吗？**
A: Zeabur 是海外平台，如果需要国内域名备案，建议使用国内部署方案。

**Q: 如何备份数据库？**
A: 可以使用 Zeabur 提供的备份功能，或通过命令行手动导出数据。

### 为什么推荐 Zeabur？

相比其他部署方案，Zeabur 特别适合本项目的原因：

1. **技术栈完全匹配**: 原生支持 Python FastAPI、React、MySQL
2. **部署零门槛**: 无需学习 Docker、Nginx 等复杂技术
3. **国内访问快**: 相比 Vercel/Netlify，Zeabur 在国内访问速度更优
4. **开发体验好**: 自动预览环境、实时日志、一键回滚
5. **性价比高**: 免费额度充足，个人项目完全够用

### 快速开始

```bash
# 1. 克隆项目
git clone your-repo-url
cd personal-knowledge-base

# 2. 创建 Zeabur 配置文件
cat > zeabur.yml << 'EOF'
type: background

mysql:
  type: mysql
  version: "8.0"

backend:
  type: container
  dockerfile: ./backend/Dockerfile
  depends_on:
    - mysql

frontend:
  type: container
  dockerfile: ./frontend/Dockerfile
  depends_on:
    - backend
EOF

# 3. 提交到 GitHub
git add .
git commit -m "Add Zeabur deployment config"
git push origin main

# 4. 在 Zeabur 控制台部署
# 访问 https://zeabur.com 开始部署
```

---

## 📝 总结

这是一个功能完整、架构清晰的全栈项目，适合作为个人作品集项目或学习全栈开发的实战案例。项目涵盖了从前端界面到后端 API、从数据库设计到系统部署的完整开发流程，具有很高的学习和实践价值。

**项目特色：**

✅ **现代化技术栈** - React 18 + FastAPI + Ant Design 5.x
✅ **完整的主题系统** - 支持明暗主题切换，跟随系统自动切换
✅ **企业级 UI 组件** - PC 端 Ant Design + 移动端 Ant Design Mobile
✅ **完美移动端适配** - 响应式布局、PWA 支持、触摸优化
✅ **高性能后端** - FastAPI 异步支持，性能提升 2-3 倍
✅ **一键部署方案** - Zeabur 平台零配置部署
✅ **完整的功能模块** - 用户认证、笔记管理、分类标签、搜索统计

**预计开发时间**: 10-15 天
**难度等级**: 中级
**推荐指数**: ⭐⭐⭐⭐⭐
**推荐部署方案**: Zeabur（个人项目首选）

---

## 🎯 项目亮点展示

### 技术亮点
- ⚡ FastAPI 高性能异步框架
- 🎨 Ant Design 5.x Design Tokens 系统
- 📱 Ant Design Mobile 移动端组件
- 🌓 完整的明暗主题方案
- 🔐 JWT + bcrypt 安全认证
- 📦 SQLAlchemy ORM + Alembic 迁移
- 🔄 PWA 离线支持
- 📲 虚拟滚动优化

### 功能亮点
- 📝 Markdown 编辑器
- 🔍 全文搜索功能
- 🏷️ 分类标签双维度管理
- 📊 数据统计可视化
- 🎯 响应式三栏布局
- ⚙️ 主题自定义配置
- 📱 移动端底部导航
- 👆 触摸手势支持
- ⬇️ 下拉刷新 & 上拉加载

### 用户体验亮点
- 🌈 平滑主题切换动画
- 💾 主题偏好持久化
- 🌙 跟随系统自动切换
- 📱 完美移动端适配
- ⌨️ 快捷键支持
- 👆 触摸手势优化
- 📲 可安装到桌面
- 🌐 离线访问支持
